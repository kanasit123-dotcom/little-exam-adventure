{
  "name": "little-exam-adventure",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "check": "npm run test && npm run build",
    "voice": "node scripts/voice-texts.mjs && python design/voice.py",
    "assets": "node scripts/asset-manifest.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^8.3.1"
  },
  "description": "Lily Exam Adventure — เกมฝึกทำข้อสอบเข้า ป.1"
}
