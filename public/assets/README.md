# Assets

Only approved, self-contained assets belong here. Do not make the new app load files from the sibling `game-lilly` repository at runtime.

Planned registry IDs and source candidates are documented in `docs/ASSET-MIGRATION.md`. Copy an asset only after recording its source commit, output size, alt text, and whether it is needed offline.

